name: Validate config

on: [push, pull_request]

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate config.toml
        run: python3 -c "import tomllib; tomllib.load(open('config/herdr/config.toml','rb'))"
